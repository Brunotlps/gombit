# Build context is this directory; keep local dev/runtime cruft and secrets out
# (vendor/ is regenerated by composer install in the image; .env is local-only).
.git
vendor
node_modules
.env
storage/logs/*.log
storage/framework/cache/data/*
*.log
